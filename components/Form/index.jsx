import React from 'react';

import * as S from './styled';

const Form = () => (
  <S.FormContainer className="flex flex-1">
    <S.FormWrapper>
    </S.FormWrapper>
  </S.FormContainer>
);

export default Form;
