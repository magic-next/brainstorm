import styled from 'styled-components';

export const CategoryWrapper = styled.article`
  width: 100%;
  margin-bottom: 1rem;
  font-size: 1.4rem;
  .category-title {
    font-weight: 500;
    font-style: italic;
    margin-bottom: .25rem;
  }
  .cards-category {
    &__card-count {
      margin-right: .5rem;
    }
  }
`;
